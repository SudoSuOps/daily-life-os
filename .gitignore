# secrets — never commit
.env
.dev.vars
*.key
*.pem
# os / editor
.DS_Store
*.swp
.idea/
.vscode/
# data / PHI — never in the repo (the firewall extends to git)
vault/
*.vault
data/
*.jsonl
node_modules/

# python caches
__pycache__/
*.pyc
